<?xml version="1.0" encoding="utf-8"?>
<rss
        version="2.0">
    <channel>
        {% for name, value in rss.simpleElements %}
            <{{ name }}>{{ value }}</{{ name }}>
        {% endfor %}
